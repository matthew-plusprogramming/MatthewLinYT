// Array, printing, pushing, removing, looping

let emailList = [];

emailList.push('example@example.com');
emailList.push('example@example2.com');

for (let i = 0; i < emailList.length; i++) {
  console.log(emailList[i]);
}
